<h1>All Comments</h1>


<%= link_to 'Home', root_path %>


<table>
  <tr>
    <th>Comments</th>
  </tr>
  <% @comments.each do |comment| %>
    <tr>
      <td><%= comment.message %>, <%= comment.created_at.asctime %></td>
    </tr>
  <% end %>
</table>
